# Supabase credentials are now provided at runtime via the Credentials tab.
# These env values are only used as a fallback if no credentials are saved.
VITE_SUPABASE_PROJECT_ID=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
VITE_SUPABASE_URL=""
